<div class="container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">

          <h3 class="card-title">Register or Login</h3>
          <p>
            <label for="Username">Username</label>
            {{!-- ***id needs to be changed --}}
            <input type="text" id="item" required="required">
          <p>
          <p>
            <label for="Password">Password</label>
            {{!-- ***id needs to be changed --}}
            <input type="text" id="item" required="required">
          <p>
            {{!-- ***I don't think we need line 19 and 20 --}}
            {{!-- <strong>Description:</strong> {{example.description}}</p>
            <a href="/">← Back To Home</a> --}}
        </div>
      </div>
    </div>
  </div>
</div>
{{!-- <div id="home">
  <div class="container">
    <div class="row">
      <div class="col-8 offset-2">
        <h3>Available:</h3><br>
        <ul>
   
          {{#each Upload}}
          <li>
            <b>{{this.product_name}} </b> <br>
            {{this.description}} <br>
             <img src={{this.Image.url}} id="img-preview" />
            <br>
            <button class="btn btn-danger float-right delete">trade</button>
            <br><br>
            <hr>
          </li>
          {{/each}}
        </ul>
      </div>
    </div>

    <button id="moveToUploadPage">upload</button>


  </div>
</div>
<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="/js/index.js"></script> --}}